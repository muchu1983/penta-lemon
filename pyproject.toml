[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "better-diet-lemon"
version = "0.1.0"
requires-python = ">=3.13"
authors = [{ name="muchu hsu", email="muchu1983@gmail.com" }]

[project.scripts]
better_diet_lemon = "better_diet_lemon.main:main"
